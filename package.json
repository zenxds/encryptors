{
  "name": "encryptors",
  "version": "0.1.0",
  "author": "zenxds",
  "devDependencies": {
    "expect.js": "~0.3.1",
    "mocha": "~2.3.3",
    "webpack": "~1.12.9"
  },
  "scripts": {
    "build": "webpack && webpack -p",
    "test": "mocha --recursive --debug --bail --timeout 30000",
    "cover": "node_modules/.bin/istanbul cover node_modules/.bin/_mocha test/*.js"
  },
  "dependencies": {
    "istanbul": "~0.4.4"
  }
}
